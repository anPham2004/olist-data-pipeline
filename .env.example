# This is an example environment file, it does not contain real credentials
# Copy it to .env and fill in the actual values before running the application
CLICKHOUSE_DB=olist_dw
CLICKHOUSE_USER=admin_placeholder
CLICKHOUSE_PASSWORD=password_placeholder